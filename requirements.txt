--extra-index-url https://download.pytorch.org/whl/cpu

# Core numeric stack
numpy==1.26.4
scipy==1.11.4

# PyTorch CPU wheels (compatible pair)
torch==2.2.2+cpu
torchvision==0.17.2+cpu

# DocTR (torch backend supplied above)
python-doctr==0.8.1
rapidfuzz==3.8.1

# Image/PDF I/O without system GL/X libs
opencv-python-headless==4.10.0.84
pypdfium2==4.30.0   # lets DocTR read PDFs without system deps

# Your libs
clean-text[gpl]==0.6.0
libretranslatepy==2.1.4
natsort==8.4.0
nltk==3.9.2
pyspellchecker==0.8.3
tqdm==4.67.1
flask==3.0.3

# Prod server
gunicorn==21.2.0
