clean-text[gpl]
libretranslatepy
natsort
nltk
pyspellchecker
torch
tqdm
rapidfuzz==2.13.7
flask
gunicorn
huggingface_hub<0.20.0
# Install opencv-python-headless FIRST
opencv-python-headless==4.10.0.84
pypdfium2==4.30.0
# Install doctr without opencv (it will use the headless version above)
python-doctr[torch]
